CROP RECOMMENDATION SYSTEM
# Crop Recommendation System
ML-based crop suggestion using Random Forest (99.55% accuracy).

## How to run locally
1. `pip install -r requirements.txt`
2. `streamlit run app.py`
